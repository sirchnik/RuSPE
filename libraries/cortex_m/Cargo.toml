# SPDX-FileCopyrightText: Infineon Technologies AG
#
# SPDX-License-Identifier: MIT

[package]
name = "cortex_m"
version = "0.1.0"
edition = "2024"

[dependencies]
tock-registers = { version = "0.10.0", default-features = false, features = ["register_types"] }

[lints]
workspace = true
